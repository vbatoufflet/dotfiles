if [ -d $HOME/.bash_completion.d/* ]; then
    for FILE in $HOME/.bash_completion.d/*; do
        source $FILE
    done
fi

if [ "$(uname -s)" == 'Darwin' -a -f $(brew --prefix)/etc/bash_completion ]; then
    source $(brew --prefix)/etc/bash_completion
fi

# vim: ft=sh ts=4 sw=4 et
